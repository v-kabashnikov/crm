<div class="btn-group <%= klass %>" role="group" aria-label="...">
  <button type="button" class="btn btn-default"><%= user.role %></button>
  <button type="button" class="btn btn-default">
    <span class="glyphicon glyphicon-user" aria-hidden="true"></span> <%= user.name %>
  </button>
  <%= link_to "mailto:#{user.email}", class: "btn btn-default" do %>
    <span class="glyphicon glyphicon-envelope" aria-hidden="true"></span> <%= user.email %>
  <% end %>
  <button type="button" class="btn btn-default">
    <span class="glyphicon glyphicon-earphone" aria-hidden="true"></span> <%= user.phone %>
  </button>
</div>
